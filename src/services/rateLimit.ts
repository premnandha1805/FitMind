import * as SQLite from 'expo-sqlite';
const db = SQLite.openDatabaseSync('fitmind.db');
const LIMITS = { gemini_per_minute: 12, gemini_per_hour: 50, gemini_per_day: 200, min_interval_ms: 4500 };
let lastRequestTime = 0;
export interface RateLimitStatus { allowed: boolean; reason: string; waitMs: number; remaining: { perMin: number; perHour: number; perDay: number }; resetAt: { perMin: string; perHour: string; perDay: string }; }
export async function checkRateLimit(type = 'gemini'): Promise<RateLimitStatus> { const now = Date.now(); const nowSec = Math.floor(now / 1000); await db.runAsync('DELETE FROM request_log WHERE timestamp < ?', [nowSec - 86400]); const perMin = await db.getFirstAsync<{count:number}>(`SELECT COUNT(*) as count FROM request_log WHERE timestamp > ? AND request_type = ? AND source = 'api'`, [nowSec - 60, type]); const perHour = await db.getFirstAsync<{count:number}>(`SELECT COUNT(*) as count FROM request_log WHERE timestamp > ? AND request_type = ? AND source = 'api'`, [nowSec - 3600, type]); const perDay = await db.getFirstAsync<{count:number}>(`SELECT COUNT(*) as count FROM request_log WHERE timestamp > ? AND request_type = ? AND source = 'api'`, [nowSec - 86400, type]); const cntMin = perMin?.count || 0; const cntHour = perHour?.count || 0; const cntDay = perDay?.count || 0; const remaining = { perMin: LIMITS.gemini_per_minute - cntMin, perHour: LIMITS.gemini_per_hour - cntHour, perDay: LIMITS.gemini_per_day - cntDay }; const waitMs = Math.max(0, LIMITS.min_interval_ms - (now - lastRequestTime)); if (cntMin >= LIMITS.gemini_per_minute) return { allowed: false, reason: `Rate limit: ${cntMin} calls in last minute`, waitMs: 60000 - ((nowSec % 60) * 1000), remaining, resetAt: { perMin: new Date(now + 60000).toLocaleTimeString(), perHour: new Date(now + 3600000).toLocaleTimeString(), perDay: 'Midnight' } }; if (cntHour >= LIMITS.gemini_per_hour) return { allowed: false, reason: `Rate limit: ${cntHour} calls in last hour`, waitMs: 3600000 - ((nowSec % 3600) * 1000), remaining, resetAt: { perMin: new Date(now + 60000).toLocaleTimeString(), perHour: new Date(now + 3600000).toLocaleTimeString(), perDay: 'Midnight' } }; return { allowed: true, reason: 'OK', waitMs, remaining, resetAt: { perMin: new Date(now + 60000).toLocaleTimeString(), perHour: new Date(now + 3600000).toLocaleTimeString(), perDay: 'Midnight' } }; }
export async function logRequest(type: string, source: string, durationMs: number, success: boolean, errorCode?: string): Promise<void> { await db.runAsync(`INSERT INTO request_log (timestamp, request_type, source, duration_ms, success, error_code) VALUES (?, ?, ?, ?, ?, ?)`, [Math.floor(Date.now()/1000), type, source, durationMs, success ? 1 : 0, errorCode || null]); if (source === 'api') lastRequestTime = Date.now(); }
export async function getRateLimitStats(): Promise<any> { const nowSec = Math.floor(Date.now() / 1000); const total = await db.getFirstAsync<{count:number}>('SELECT COUNT(*) as count FROM request_log'); const fromCache = await db.getFirstAsync<{count:number}>(`SELECT COUNT(*) as count FROM request_log WHERE source='cache'`); const fromApi = await db.getFirstAsync<{count:number}>(`SELECT COUNT(*) as count FROM request_log WHERE source='api'`); const fromFallback = await db.getFirstAsync<{count:number}>(`SELECT COUNT(*) as count FROM request_log WHERE source='fallback'`); const t = total?.count || 1; return { total: t, fromCache: fromCache?.count || 0, fromApi: fromApi?.count || 0, fromFallback: fromFallback?.count || 0, cacheHitRate: (((fromCache?.count || 0) / t) * 100).toFixed(1) + '%', apiCallsToday: await db.getFirstAsync<{count:number}>(`SELECT COUNT(*) as count FROM request_log WHERE timestamp > ? AND source='api'`, [nowSec - 86400]).then(r => r?.count || 0) }; }
